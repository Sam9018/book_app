<!DOCTYPE html>
<html lang="en">
<head>
        <%- include('../../layout/head') %>
</head>
<body>
    <header><%- include('../../layout/header') %></header>
    <nav> <%= item.length %> Saved Books</nav>
    <main>
        <%- include('./detail',{item:item, formAction:'create'}) %>
        <button value = "<%=item.id %>" 
                class="dropForm"
                name="update">
                Update Details
        </button>

        <%- include('./edit', {id: item.id, item:item, formAction:'update'}) %>
        <button class="deleteBook" value = "<%=item.id %>" name="delete">
                Delete Book
        </button>    
    </main>

    <footer>
            <%- include('../../layout/footer') %>
    </footer>

</body>
</html>